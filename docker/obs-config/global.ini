[General]
FirstRun=false

[OBSWebSocket]
ServerEnabled=true
ServerPort=4455
# AuthRequired=false is fine for Docker's internal network.
# To enable auth: set AuthRequired=true and ServerPassword below,
# then set OBS_WEBSOCKET_PASSWORD to the same value in docker-compose.
AuthRequired=false
ServerPassword=
AlertsEnabled=false
